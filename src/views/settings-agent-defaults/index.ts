// Settings: Agent Defaults — orchestration + public API
export { loadAgentDefaultsSettings } from './molecules';

export function initAgentDefaultsSettings() {
  // All dynamic — no static event wiring needed
}
